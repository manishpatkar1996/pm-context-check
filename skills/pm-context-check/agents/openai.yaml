interface:
  display_name: "PM Context Check"
  short_description: "Review PM context efficiency after a Codex task"
  default_prompt: "Use $pm-context-check to review how efficiently I used context in my previous product-management task."
policy:
  allow_implicit_invocation: false
